// STUB screen. Replaced by its owning agent. Keep the export name and props.
export function Playground(props: Record<string, unknown>) {
  return <div class="empty-state">Playground screen is being built. {JSON.stringify(props)}</div>;
}
