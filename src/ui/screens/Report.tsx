// STUB screen. Replaced by its owning agent. Keep the export name and props.
export function Report(props: Record<string, unknown>) {
  return <div class="empty-state">Report screen is being built. {JSON.stringify(props)}</div>;
}
