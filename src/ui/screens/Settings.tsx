// STUB screen. Replaced by its owning agent. Keep the export name and props.
export function Settings(props: Record<string, unknown>) {
  return <div class="empty-state">Settings screen is being built. {JSON.stringify(props)}</div>;
}
