// STUB screen. Replaced by its owning agent. Keep the export name and props.
export function MidsemTest(props: Record<string, unknown>) {
  return <div class="empty-state">MidsemTest screen is being built. {JSON.stringify(props)}</div>;
}
