// STUB screen. Replaced by its owning agent. Keep the export name and props.
export function TopicPage(props: Record<string, unknown>) {
  return <div class="empty-state">TopicPage screen is being built. {JSON.stringify(props)}</div>;
}
