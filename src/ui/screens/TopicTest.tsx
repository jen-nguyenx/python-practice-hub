// STUB screen. Replaced by its owning agent. Keep the export name and props.
export function TopicTest(props: Record<string, unknown>) {
  return <div class="empty-state">TopicTest screen is being built. {JSON.stringify(props)}</div>;
}
