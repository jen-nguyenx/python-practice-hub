// STUB screen. Replaced by its owning agent. Keep the export name and props.
export function Landing(props: Record<string, unknown>) {
  return <div class="empty-state">Landing screen is being built. {JSON.stringify(props)}</div>;
}
